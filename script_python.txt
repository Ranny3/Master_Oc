---------------------------------Python---------------------------------
! python nomedoarquivo.py % carrega o script com o arquivo na pasta
execfile ("nomedoarquivo.py") %executa

# Open the script file in read mode
with open('nomedoarquivo.py', 'r') as f:
    # Read the contents of the file
    content = f.read()
    # Print the contents to the console
    print(content)
-------------------------------------------------------------------------

